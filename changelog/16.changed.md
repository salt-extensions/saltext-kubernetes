API responses now return ``camelCase`` keys matching Kubernetes YAML manifests instead of ``snake_case``, and ``_present`` state functions use ``patch`` instead of delete-and-recreate (the ``replace`` parameter has been removed).
