apiVersion: v1
kind: Service
metadata:
  name: {{ context.name }}
spec:
  selector:
    app: {{ context.name }}
  ports:
    - protocol: TCP
      port: {{ context.port }}
      targetPort: {{ context.target_port | default(context.port) }}
  type: {{ context.type | default('ClusterIP') }}
